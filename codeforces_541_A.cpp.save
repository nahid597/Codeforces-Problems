
#include<bits/stdc++.h>

using namespace std;

int main()
{
    int n;

   // vector < int > vec(1000000001);

    cin >> n;

    int x[n+1];
    long long vec[1000000];

    for(int i = 0; i < n; i++)
    {
        cin >> x[i];

       // vec[x[i]] = 0;

       // vec.push_back(x[i]);
       // vec.at(x[i])=0;
       // y[x[i]] = 0;
       // cout << y[x[i]] << " ";
    }

    sort(x, x + n);


    for(int i = 0; i < n; i++)
    {
       if(x[i] % 2 == 0)
          cout << x[i] << " ";
    }

    for(int i = n - 1; i > 0; i--)
    {
       if(x[i] % 2 = 0)
       {
          cout << x[i] << " ";
       }
    }

    cout << endl;

   // vec.clear();


    return 0;
}
